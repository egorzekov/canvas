import Canvas from "./Canvas";

// window.HTMLCanvasElement.prototype.getContext = jest.fn(() => {
//     rect: jest.fn();
// });

describe("Canvas", () => {
    it("should render rect", () => {

    });
});
